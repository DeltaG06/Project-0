package com.gaurav.queon.ui.theme

import androidx.compose.ui.graphics.Color

// --- Gemini Aesthetic Palette ---

// Backgrounds
val DeepSpaceBlack = Color(0xFF03030A)
val DeepSpaceBlue = Color(0xFF0A0E17)
val SurfaceGlass = Color(0xFF131629)

// Accents (Neon/Electric)
val ElectricBlue = Color(0xFF4B90F9)
val ElectricPurple = Color(0xFFD458F8)
val NeonCyan = Color(0xFF00E5FF)

// Functional (Focus Mode)
val FocusGreen = Color(0xFF00E676)
val DangerRed = Color(0xFFFF1744)
val WarningAmber = Color(0xFFFFC107)

// Text
val TextPrimary = Color(0xFFFFFFFF)
val TextSecondary = Color(0xFFB0B0C0)
val TextTertiary = Color(0xFF6E6E85)

// Gradients (for Brush use)
val GeminiGradientStart = ElectricBlue
val GeminiGradientEnd = ElectricPurple